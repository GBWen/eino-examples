version: "3.8"

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      # Persist data locally; change ./qdrant_data to another path if you prefer.
      - ./qdrant_data:/qdrant/storage
    restart: unless-stopped


